services:
  orchestrator:
    build:
      context: orchestrator
    ports:
      - "8080:8080"
    environment:
      - CAMERA_URLS=test=rtsp://mediamtx:8554/test
    depends_on:
      - mediamtx

  mediamtx:
    image: bluenviron/mediamtx:latest
    ports:
      - "8554:8554"
